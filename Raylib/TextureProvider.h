//#pragma once
//
//
//
//
//// provides &texture, origin, and source when given graphicsEnum
//class TextureProvider
//{
//public:
//	// stores a reference to the graphics container. Have to do it so that graphicsType can be called from anywhere and so don't waste time
//	// resetting the graphicsContainer needlessly every frame. besides there will only ever be one graphicsContainer
//	TextureProvider(std::vector<std::unique_ptr<Graphic>> const & graphicsContainer_);
//	~TextureProvider();
//public:
//	Texture2D const & get_texture(GraphicsTypes graphicsType_); // TODO make it clear this is an enum
//private:
//public:
//
//private:
//};

